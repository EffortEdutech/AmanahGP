// NOTE: Tailwind v4 with @tailwindcss/postcss does NOT read this file.
// Content detection is automatic in v4. Custom design tokens live in globals.css @theme blocks.
// This file is kept as a safe stub to avoid TypeScript import errors from any tooling.
import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
  theme: { extend: {} },
  plugins: [],
};

export default config;
